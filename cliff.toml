# --------------------------------------------------------------------
# cliff.toml
# --------------------------------------------------------------------
# This file configures git-cliff changelog generation for the
# RAIL Portal Plugin.
#
# The build-and-deploy script depends on this configuration.
#
# Commit messages MUST follow Conventional Commits format:
#   type(scope): description
#
# Example:
#   feat(nav): Redirect Refined Links
#   fix(ui): Correct sidebar hover state
#
# Only conventional commits are included in CHANGELOG.md.
# Release commits (chore(release): ...) are intentionally skipped.
#
# If changelog formatting needs to change,
# update this file — NOT CHANGELOG.md directly.
# --------------------------------------------------------------------


[changelog]
# changelog header
header = """
# Changelog\n
All notable changes to the RAIL Portal Plugin will be documented in this file.\n
"""
# template for the changelog body
# https://keats.github.io/tera/docs/#introduction
body = """
{% if version %}\
    ## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
    ## [unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
    ### {{ group | upper_first }}
    {% for commit in commits %}
        - {% if commit.scope %}*({{commit.scope}})* {% endif %}{{ commit.message | upper_first }}\
{% if commit.body %}
    > {{ commit.body | trim | replace(from="\n", to="\n  >\n  > ") }}
{% endif %}
    {% endfor %}
{% endfor %}\n
"""
# template for the changelog footer
footer = """
"""
# remove the leading and trailing whitespace from the templates
trim = true

[git]
# parse the commits based on https://www.conventionalcommits.org
conventional_commits = true
# filter out the commits that are not conventional
filter_unconventional = true
# process each line of a commit as an individual commit
split_commits = false
# regex for parsing and grouping commits
commit_parsers = [
    { message = "^feat", group = " Features" },
    { message = "^fix", group = " Bug Fixes" },
    { message = "^doc", group = " Documentation" },
    { message = "^perf", group = "⚡ Performance" },
    { message = "^refactor", group = " Refactor" },
    { message = "^style", group = " Styling" },
    { message = "^test", group = "離 Testing" },
    { message = "^chore\\(release\\): prepare for", skip = true },
    { message = "^chore", group = "⚙️ Maintenance" },
    { message = ".*", group = "⭐ Other" },
]
# protect breaking changes from being skipped due to matching a skipping commit_parser
protect_breaking_commits = false
# filter out the commits that are not matched by commit_parsers
filter_commits = false
# glob pattern for matching git tags
tag_pattern = "v[0-9]*"
# sort the tags topologically
topo_order = false
# sort the commits inside sections by oldest/newest order
sort_commits = "oldest"



The cliff.toml above creates the changelog below but a few things are not how I want. 

Currently, the quoteblocks related to the commit subject line (not the body) are not indented under the subject line bullet. For example, 

v.2.0.19 looks like this: 

## [2.0.19] - 2026-03-27

###  Bug Fixes

- *(ui)* Update request icon urls
> -Updated the request icon URLs to the "public" customer facing URLs
>
> -The icon URLs currently used do not render for users without a license

When I want it to look like this: 

## [2.0.19] - 2026-03-27

###  Bug Fixes

- *(ui)* Update request icon urls
  > -Updated the request icon URLs to the "public" customer facing URLs
  >
  > -The icon URLs currently used do not render for users without a license


Additionally, there is an extra space being added after each bullet point and two extra spaces added after each quote block and I don't want that happening either. How can I change this cliff.toml to handle this?

# Changelog

All notable changes to the RAIL Portal Plugin will be documented in this file.

## [2.0.19] - 2026-03-27

###  Bug Fixes

- *(ui)* Update request icon urls
> -Updated the request icon URLs to the "public" customer facing URLs
>
> -The icon URLs currently used do not render for users without a license



## [2.0.18] - 2026-03-27

###  Documentation

- *(build-deployment)* Updating docs to include info on the dev build and deploy script


### 離 Testing

- *(build-and-deploy-dev.sh)* Adding a build script for dev packaging and testing


## [2.0.17] - 2026-03-05

###  Bug Fixes

- *(frontend)* Update landing page hero image URL
> - Hero image used previously was a Refined URL, now that we have disabled refined, you cannot see the image
>
> - Added the image to an issue in ATL, should be visible by all



###  Documentation

- *(build-and-deploy.sh)* Updating docs for the revised build script and the changelog generation


## [2.0.16] - 2026-02-27

###  Refactor

- *(request-types)* Update the format of request type groups to not span full width unless needed


## [2.0.15] - 2026-02-27

###  Bug Fixes

- *(request-types)* Update to respect the order of request type groups

- *(request-types)* Update to allow spacing between request type groups and the actual request type cards
> - Fix is for S2CMPSD bug reported in WMPR-8706



###  Refactor

- *(ui)* Updated info button on landing page
> - Changed the info button on the landing page to be a popover, prevents it being in the way of JQL table elements



